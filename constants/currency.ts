/** ISO 4217 */
export const OMR_CODE = 'OMR' as const;

/**
 * Width ÷ height of `assets/omr-symbol.png` (Wikimedia thumbnail of the official mark).
 * Keeps the icon aligned with text when sizing by height.
 */
export const OMR_ICON_ASPECT = 330 / 179;
